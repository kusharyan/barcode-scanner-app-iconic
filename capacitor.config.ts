import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.product.barcodeScanner',
  appName: 'Product Barcode Scanner App',
  webDir: 'www'
};

export default config;
